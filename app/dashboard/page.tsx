import { MessageCard } from "@/components/dashboard/message-card";

export default function Page() {
	return (
		<section className="flex flex-col items-center justify-center h-[calc(screen-150px)] ">
			<MessageCard />
		</section>
	);
}
